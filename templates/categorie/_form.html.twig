{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="mt-3">
        <button class="btn btn-sm btn-success">{{ button_label|default('Créer une catégorie') }}</button>
        <a class="btn btn-sm btn-primary" href="{{ path('categorie_index') }}">Liste des catégories</a>
    </div>
{{ form_end(form) }}
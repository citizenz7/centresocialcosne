{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-sm btn-warning">{{ button_label|default('Modifer le profil') }}</button>
    <button class="btn btn-sm btn-secondary" type="reset">Annuler</button>
{{ form_end(form) }}

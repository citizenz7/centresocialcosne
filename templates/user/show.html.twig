{% extends 'base.html.twig' %}

{% block title %}{{ site_name_full }} : profil {{ app.user.prenom }} {{ app.user.nom }}{% endblock %}

{% block body %}
<main id="main">

    <!-- ======= Breadcrumbs ======= -->
    <section class="breadcrumbs">
        <div class="container">
            <h2>Administration du site</h2>
        </div>
    </section>
    <!-- End Breadcrumbs -->

    <section class="inner-page">
        <div class="container">
            <div class="row d-flex justify-content-center">
                <div class="col">
                    <h2>Profil de : {{ app.user.prenom }} {{ app.user.nom }}</h2>

                    <table class="table">
                        <tbody>
                            <tr>
                                <th>Email</th>
                                <td>{{ user.email }}</td>
                            </tr>
                            <tr>
                                <th>Prenom</th>
                                <td>{{ user.prenom }}</td>
                            </tr>
                            <tr>
                                <th>Nom</th>
                                <td>{{ user.nom }}</td>
                            </tr>
                            <tr>
                                <th>Roles</th>
                                {% if user.roles|json_encode == '["ROLE_USER"]' %}
                                <td><span class="text-primary fw-bold">Utilisateur</span></td>
                                {% else %}
                                <td><span class="text-danger fw-bold">Admin</span></td>
                                {% endif %}
                            </tr>
                            <tr>
                                <th>Fonctions au Centre Social</th>
                                <td>{{ user.fonction }}</td>
                            </tr>
                            <tr>
                                <th>Image de profil</th>
                                <td><img src="../uploads/images/users/{{ user.image }}" class="img-fluid" style="max-width:100px; width:100%;" alt="{{ user.prenom }}"</td>
                            </tr>
                            <tr>
                                <th>Compte vérifié</th>
                                <td>{{ user.isVerified ? 'Oui' : 'Non' }}</td>
                            </tr>
                        </tbody>
                    </table>

                    <table>
                        <tr>
                            <td><a class="btn btn-sm btn-warning"
                                    href="{{ path('user_edit', {'id': user.id}) }}">Modifier votre profil</a></td>
                            <td>{{ include('user/_delete_form.html.twig') }}</td>
                        </tr>
                    </table>
                </div>
            </div>
        </div>
    </section>
</main>

{% endblock %}